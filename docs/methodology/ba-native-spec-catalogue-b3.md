# Geniusee Technique Catalogue — Batch B3
### BA-Native Spec · Wave 2 · Canvas-internal aspects · v0.3 — the sheets declare what they take in (21 Aug 2026; review incorporated 26 July 2026)
**v0.3 change record:** one template amendment, ruled 21 Aug 2026 (**R3** of the EC-18 package; origin: the field defect report of 20 Aug 2026, **Part B item B7** — electing T-12 alone silently pulled in `domain-model.md` (T-11 output) and `constitution.md` (T-15 output), and the dependency was declared nowhere the election could see it): **every sheet in this batch gains the §2 metadata's fifth field, `Preconditions`** (sequencing plan v0.5 §2) — the other techniques' artifacts this sheet's own contract and template text demand, each with its producing technique and its grade, **hard** (a contracted cell cannot be authored without it) or **soft** (one input among several; absence costs candidates, not cells), and **`—` where the sheet takes none**, with the reason stated so a bare dash is never a silence. What the field does at an election is orchestrator law and is cited, never restated: **visibility, never a block** — the election sees what it is taking on, nothing is silently pulled in, and the run proceeds in assumption posture with the dependent cells marked (orchestrator v0.33 §6.1, **D-O84**). **T-08 declares `stakeholders.md` (T-03) hard** — §3 requires every P-line's who-hurts to resolve to a register population, the resolution target §2's own trigger already named. **T-09 declares `constraints.md` (T-06) hard** — §3 pins the AT-VI-3 scan to its Confirmed rows (D-B3-2) — **and `competitive-analysis.md` (T-07) soft**, the differentiation keying against a canvas §10 Unlike entry while the analysis file supplies the keyed deltas. T-10 declares **`—`**. **No purpose, trigger, skip-if, depth, output contract, procedure step, template, micro-example, gate hook, mining note or build-brief hook moved**, and no decision in this batch is re-ruled: the field **declares what the sheets already demanded** and adds no demand of its own.


**What this is:** sheets T-08–T-10 of the 18-sheet catalogue (sequencing plan §1), authored to the §2 uniform template in the pinned within-batch order: value → vision → solution. B3 is the DAG's middle — the canvas-internal trio (D-W1): the three aspects whose evidence home is the canvas itself (orchestrator §2.1) get catalogue coverage without any new artifact — each sheet completes its own canvas sections, in DAG order, one sitting (plan §3, B3 rationale): vision consumes B2's planted seeds (AT-VI-3 ← `constraints.md` Confirmed rows, D-B2-4; AT-VI-2 ← Unlike entries + keyed deltas, D-B2-3), and solution links functions to session-fresh objectives (AT-SO-2) and pins the entity ground T-11 (B4) opens with — the batch's forward hand-off (D-B3-5, locked).

**Rulings in force (restated once):** every sheet authored from scratch; presale skills are reference designs to mine, never components (elicitation v0.3 §7/§11). No ★ sheet exists in this batch — all three follow the elicitation-§7.1 no-reference pattern (the presale canvas skill's section heuristics were mined once, at T-01, catalogue-b1 §7 — framing surface, not aspect procedure). Micro-examples stay in the appointment-booking world at its **pre-RO-1 state** (catalogue-b1 convention): Specialists self-publish availability, no Clinic Admin yet. Class cell `(canvas-internal)` per the inventory: each pinned contract's artifact class is the canvas's own — Context, #1 — and **no new file joins the CC-H-01 estate**. Prior-batch sheets (T-01–T-03, catalogue-b1 v0.2 · T-04–T-07, catalogue-b2 v0.2) are cited, never restated. One pattern sharpens down the DAG: the contradiction asymmetry (catalogue-b1 T-03 step 5) does increasing work — by T-10 every upstream aspect is cleared, and a contradiction found is reopen-signal ground. Decisions D-B3-1…D-B3-5 are locked (review record below); the locked text stands inline at each sheet.

---

## T-08 · Value definition
Band 1 · serves: Value · class: (canvas-internal) · target: canvas.md — Problems · Objectives sections (#1) · ★ —

### 1. Purpose & BABOK grounding
Completes the canvas Problems and Objectives sections at aspect grade — the value case as pointable lines: every problem naming who hurts, resolved against the register's populations; every objective linked to the problem it answers and stated concretely enough to be cited downstream. This is the first link of the chain CC-OV-02 later walks (canvas → brief §1 → spec Overview, elicitation §4): a value claim that cannot be pointed at here cannot anchor a feature there. The decision this technique lets the BA make: the value case is real and pointable — who hurts, what changes, and how the two connect — so Vision work differentiates a stated value, not a vibe. BABOK: 6.1 Analyze Current State · 6.2 Define Future State · 10.40 Root Cause Analysis · 7.3 Validate Requirements · 4.3 Confirm Elicitation Results. House: plan Q1a/§3 (Value ↔ Problems · Objectives); AT-VA-1/-2 wording; D-B1-1 line-IDs (catalogue-b1, T-01 — cited, never restated).

### 2. Catalogue metadata (BA-planning loop — orchestrator §6)
| Field | Value |
|---|---|
| Serves | Value |
| Evidence triggers | AT-VA-1 — canvas Problems empty or `open` · a P-line names no who-hurts · a named who-hurts resolves to no register population (the resolution target: T-03's table, catalogue-b1). AT-VA-2 — Objectives empty or `open` · an O-line carries no `→ P-n` link · an O-line too vague to be cited downstream (the cannot-be-pointed-at pattern). |
| Skip-if | AT-VA-1 and AT-VA-2 read met in the current evidence table (e.g., a presale canvas arrived with who-hurts and linkage already at aspect grade and the resolution pass confirms it). Enrichment beyond the threshold — benefit quantification, KPI trees — only on BA ask (§6.1). |
| Depth | Elicits at value grade: per problem — one line, who hurts, the evidence behind the pain where sourced; per objective — one line, `→ P-n`, a baseline or measure where the material gives one. Must NOT descend into solution capabilities (T-10 ground — "what it does" answers Solution, not Value) · differentiation claims (T-09) · KPI frameworks or benefit-realization models (enrichment, BA ask) · acceptance thresholds or NFR targets (spec ground, Tier 2) · register authoring (an unresolved who-hurts is a routed register proposal — T-03's ground, never an inline cast edit). |
| Preconditions | `.specify/memory/stakeholders.md` (**T-03**) — **hard**: §3 requires every P-line's who-hurts to **resolve to a register population** (the resolution target named in §2's own trigger), so without the register each who-hurts is a mark rather than a resolution. The DAG makes the absence rare, not impossible — Value opens off Stakeholders (§3.1), and a waived root leaves the register thin; the run then drafts and marks (orchestrator §6.1, D-O84). |

### 3. Output contract (Q2+ — pre-pinned; orchestrator §6.3)
| Field | Value |
|---|---|
| Expected output | Canvas Problems and Objectives completed at aspect grade: every P-line naming who-hurts resolving to register populations; every O-line linked `→ P-n` and citable-concrete; line-IDs per D-B1-1 (catalogue-b1), new lines ID'd by continuation (D-B3-1, locked) |
| Artifact class | Context — the canvas's own class (#1, canvas-internal: this run completes sections of the framed canvas; no new artifact) |
| Destination file | `canvas.md` — §2 Problems · §12 Objectives (repo root, framed by T-01; in the CC-H-01 estate from arming) |

### 4. Procedure
1. **BA act:** open Value (T1 — Stakeholders cleared or waived); compose the plan (P-O2); invoke T-08 (P-O3).
2. **Framework act:** pre-draft section completion from the framing-grade lines (IDs carried per D-B1-1), presale material, kickoff notes, and routed arrivals; cite-or-mark per line (principle 3). New problem or objective candidates take continuation IDs — the next unused `P-n`/`O-n`; a retired ID is never reused (D-B3-1, locked).
3. **Framework act:** resolution + linkage pass — per P-line, the named who-hurts diffed against register populations (AT-VA-1's resolution target); per O-line, a `→ P-n` link proposed with its basis where the material implies but does not state one (framing wrote only stated links — catalogue-b1 T-01 step 3; completion is exactly this run's ground); a concreteness check flags any O-line too vague to cite.
4. **Framework act:** remaining holes become destination-tagged questions — a P-line's who-hurts or evidence, an O-line's link, baseline, or concreteness (principle 2; threshold grade bounds the set, no numeric cap needed).
5. **BA act:** rule — confirm or edit lines, rule proposed links, rule each who-hurts resolution. Boundary routing + asymmetry: a newly named population is a pure addition — a proposed register (and canvas Customers) edit via §3.5 (T-03 §6's landing row); a finding contradicting the cleared cast is a reopen signal (§3.5 step 4 → P-O6). A candidate that is really a capability is proposed to canvas §7 by batch — Solution's owning pass completes it later (arrival is never gated, §2.2).
6. **Framework act:** write §§2, 12 under the contract; refresh the Value evidence table (§7.4) — AT-VA-1/-2; confirmation is proposed when the table reads met (with Stakeholders' clearing standing, Value's clearing supplies Vision's second edge).

### 5. Output template & micro-example
```markdown
| # | Section | Content — aspect grade |
|---|---|---|
| 2 | Problems | P-n — one line each: the pain, who hurts (register populations), evidence where sourced |
| 12 | Objectives | O-n — one line each: the change sought, `→ P-n`, baseline/measure where sourced |
```
Micro-example — Value first pass (pre-RO-1):

| # | Section | Content |
|---|---|---|
| 2 | Problems | P-1 — ~30% of booking calls go unanswered; bookings are lost — hurts: Clients (cannot get through to book) · Specialists (booked work never lands) `[presale canvas · kickoff notes]` |
| 12 | Objectives | O-1 — Take booking self-serve online for the network, MVP this year → P-1 `[presale brief]` · O-2 — Reduce lost bookings → P-1; baseline: the ~30% unanswered-call rate `[presale canvas · kickoff notes]` |

Continuity: the two lines are the kit-baseline citations' exact targets (`[canvas: Problems P-1]` · `[canvas: Objectives O-2]`, elicitation §8.1), and O-2 is the line the E-03 Value Anchor cites (elicitation §8.2 §1) — the CC-OV-02 chain, walked backward. Who-hurts resolves to the register's populations (catalogue-b1 T-03) — AT-VA-1's form; the P-1 baseline carried into O-2 is what "concretely enough to be cited" buys.

### 6. Gate hooks
| Hook | IDs |
|---|---|
| Threshold hooks | AT-VA-1 · AT-VA-2 |
| Health hooks | CC-H-01 — via `canvas.md`, in the estate since T-01's framing (catalogue-b1); this sheet adds no file |
| Consumer hooks | CC-OV-02 — chain anchor: the spec's value claim resolves to these `P-n`/`O-n` lines directly or through brief §1, which itself resolves here (elicitation §4: canvas → brief → spec); this run is what makes the anchor citable rather than merely present |

Cross-layer consumers, on the record: Tier-1 kit baselines cite these lines (elicitation §8.1) · brief §1 Value Anchor resolves here by name (elicitation §4) · AT-SO-2's linkage (T-10, this batch) points functions at these O-lines · competitive deltas key to them (catalogue-b2 T-07).

### 7. Reference-design mining notes
No reference design — built to this sheet alone (elicitation §7.1 pattern).

### 8. Phase-2 build-brief hook
Invocation: `/ba-run T-08`; contract check at P-O3. Inputs loaded: `canvas.md` (Problems · Objectives first) · `.specify/memory/stakeholders.md` (the resolution target) · presale material and kickoff notes · `glossary.md`. Interaction: pre-draft → resolution + linkage pass → destination-tagged questions → BA rulings → write + routed batches. Outputs written: `canvas.md` §§2, 12, plus register/Customers batches where resolution demands them. §§2–5 above fix the method; Phase 2 adds the who-hurts ⇄ register diff rendering, continuation-ID assignment (D-B3-1), and the evidence-table refresh wiring.

---

## T-09 · Vision & differentiation
Band 1 · serves: Vision · class: (canvas-internal) · target: canvas.md — Product→The/Is/That · Competition→Our Solution sections (#1) · ★ —

### 1. Purpose & BABOK grounding
Authors the vision at statement grade: the three-slot product statement (The / Is / That) and the Our Solution differentiation — against named Unlike entries, leaning on the competitive table's keyed deltas — then runs the constraint-contradiction scan: every Confirmed constraint row read once against the statement, every hit resolved or named before the aspect can clear. Consumes B2's planted seeds and authors what T-07 deliberately does not: the statement itself (catalogue-b2 T-07 depth rule — supplies targets and deltas, never the statement). The decision this technique lets the BA make: one product statement — complete, differentiated against something named, and not at war with what binds. BABOK: 6.2 Define Future State · 10.8 Business Model Canvas · 7.2 Verify Requirements (consistent — the scan) · 4.3 Confirm Elicitation Results. House: plan §3 (Vision ← Context + Value — the DAG's only two-edge gate); AT-VI-1/-2/-3 wording; D-B2-3 (competitive file) · D-B2-4 (Status column — the scan's mechanical read), catalogue-b2.

### 2. Catalogue metadata (BA-planning loop — orchestrator §6)
| Field | Value |
|---|---|
| Serves | Vision |
| Evidence triggers | AT-VI-1 — a Product slot (The / Is / That) empty or `open`. AT-VI-2 — Our Solution absent, or stating differentiation against no named Unlike entry (statement side only; a missing target — zero Unlike entries, entries without deltas — is T-07's hole under the Vision plan, catalogue-b2 §2). AT-VI-3 — the scan has not run against the current statement + Confirmed-row set (either side changed since the last scan) · a found contradiction stands neither resolved nor named. |
| Skip-if | AT-VI-1…-3 read met in the current evidence table (a complete, differentiated statement carried from presale; scan clean against the current Confirmed set). Target-side work is never this sheet's ground — entries and deltas are T-07's (re-invocable under the Vision plan). |
| Depth | Elicits at statement grade: three slots, one line each; one differentiation statement naming its targets and leaning on keyed deltas. Must NOT author competitive entries or deltas (T-07) · enumerate the surface — forms, functions, connections (T-10) · rewrite value lines (T-08's ground, cleared by now — proposals route) · make technology or architecture choices dressed as vision (golden rule 1 at aspect grade — the *how* belongs to `/plan`) · expand into brand or marketing copy beyond the three slots (no decision content). |
| Preconditions | `.specify/memory/constraints.md` (**T-06**) — **hard**: §3 pins the **AT-VI-3 scan against its Confirmed rows** (D-B3-2), and a scan with no row set is not a scan; absent, the statement is written and the scan stands marked as unrun · `.specify/memory/competitive-analysis.md` (**T-07**) — **soft**: the differentiation keys against a **named Unlike entry**, which is canvas §10 ground (AT-CX-3's), and the analysis file supplies the keyed deltas rather than the names — absent, the keying stands on the canvas entries alone (orchestrator §6.1, D-O84). |

### 3. Output contract (Q2+ — pre-pinned; orchestrator §6.3)
| Field | Value |
|---|---|
| Expected output | Canvas Product→The/Is/That completed (three slots filled) and Competition→Our Solution stating differentiation against ≥ 1 named Unlike entry, keyed `→ P-n / O-n`; the AT-VI-3 scan run against `constraints.md` Confirmed rows with every hit resolved or conflict-marked (D-B3-2, locked) |
| Artifact class | Context — the canvas's own class (#1, canvas-internal: no new artifact) |
| Destination file | `canvas.md` — §§3–5 Product.The/Is/That · §11 Competition.Our Solution (repo root; in the CC-H-01 estate from arming) |

### 4. Procedure
1. **BA act:** open Vision (T1 — Context **and** Value cleared or waived, Q1b's two-edge gate); compose the plan (P-O2); invoke T-09 (P-O3).
2. **Framework act:** pre-draft the statement — The/Is/That from framing seeds, presale material, and kickoff notes; Our Solution drafted against the competitive table's entries and keyed deltas (`.specify/memory/competitive-analysis.md`, D-B2-3 — read, never authored here); cite-or-mark per line (principle 3).
3. **Framework act:** the AT-VI-3 scan — every Confirmed row of `constraints.md` (D-B2-4's mechanical read; Assumed rows excluded) read once against the drafted statement. Per hit, a proposed resolution: statement revision (the normal case), or — where the claim may stand — a conflict marker on the statement line (D-B3-2, locked — the D-B1-2 form at aspect grade) with the constraint challenge routed as a reopen signal on Context (cleared ground — §3.5 step 4 → P-O6).
4. **Framework act:** remaining holes become destination-tagged questions — an empty slot, a differentiation naming no entry (principle 2). A missing target is never authored here: the framework surfaces it as a plan-composition prompt — add T-07 under the Vision plan (its dual-aspect entry, catalogue-b2 §2) — and proceeds on what stands.
5. **BA act:** rule — the slots, the statement, each scan hit (revise · challenge → reopen signal · name-and-carry under the marker); on a missing target, re-compose the plan (P-O2) to run T-07 first.
6. **Framework act:** write §§3–5, 11 under the contract; refresh the Vision evidence table (§7.4) — AT-VI-1/-2/-3; confirmation is proposed when the table reads met. Clearing over a named conflict is legal by the criterion's own text ("resolved or named as an open conflict before clearing") — the marker stays visible on the canvas and in the clearing's evidence line (D-B3-2, locked).

### 5. Output template & micro-example
```markdown
| # | Section | Content — aspect grade |
|---|---|---|
| 3 | Product.The | name / working title |
| 4 | Product.Is | solution type, one line |
| 5 | Product.That | what it enables for the customer, one line |
| 11 | Competition.Our Solution | differentiation vs ≥ 1 named Unlike entry, keyed `→ P-n / O-n`; conflict markers where a scan hit is held (D-B3-2) |
```
Micro-example — Vision first pass (pre-RO-1):

| # | Section | Content |
|---|---|---|
| 3 | Product.The | Network Booking Portal — working title `[Olena, 2026-07-09]` |
| 4 | Product.Is | a self-serve online booking service for the clinic network |
| 5 | Product.That | lets Clients book a Specialist's published Slots — and cancel their own Appointments — without phoning the clinic |
| 11 | Competition.Our Solution | Unlike phone booking (the status quo), it is open outside clinic hours and drops no calls → P-1 · O-2; unlike MedSlot, it is the network's own channel — client relationship kept, no per-booking commission → O-1 |

Scan (AT-VI-3) — `constraints.md` Confirmed rows, read once: no-install-at-clinics — compatible (self-serve web, nothing installed at clinics) · autumn launch — compatible (O-1's MVP-this-year statement) · personal-data law — no vision claim touches data handling. Assumed rows excluded (D-B2-4). Zero hits — nothing to resolve or name.

Continuity: the differentiation names two of the three Unlike entries and leans on their keyed deltas (catalogue-b2 T-07 — AT-VI-2's "≥ 1 named" met with margin); the off-hours clause is the same belief thread the 07-14 call later confirms (elicitation §8.2 §1); the clean scan is the normal case — the D-B3-2 marker exists for the other one.

### 6. Gate hooks
| Hook | IDs |
|---|---|
| Threshold hooks | AT-VI-1 · AT-VI-2 · AT-VI-3 |
| Health hooks | CC-H-01 — via `canvas.md` (no new file) |
| Consumer hooks | — (no Scope-F assertion reads the vision sections directly) |

Cross-layer consumers, on the record: AT-VI-2's target side is T-07's supply (catalogue-b2 T-07 §6: "the statement itself clears it (T-09, B3)" — this is that sheet) · a feature needing a competitive fact cites the Our Solution line (catalogue-b2 T-07 §6 boundary) · AT-SO-2 may link functions to these vision elements by section name (T-10, D-B3-3) · reopen signals from the scan land in orchestrator §9's table (Context mapping).

### 7. Reference-design mining notes
No reference design — built to this sheet alone (elicitation §7.1 pattern).

### 8. Phase-2 build-brief hook
Invocation: `/ba-run T-09`; contract check at P-O3. Inputs loaded: `canvas.md` (Product slots, Our Solution, Problems/Objectives) · `.specify/memory/competitive-analysis.md` (entries + keyed deltas) · `.specify/memory/constraints.md` (Confirmed rows) · `glossary.md`. Interaction: statement pre-draft → AT-VI-3 scan with per-hit proposals → destination-tagged questions → BA rulings (revise · challenge → RO · name-and-carry) → write. Outputs written: `canvas.md` §§3–5, 11, plus reopen signals where a challenge stands. §§2–5 above fix the method; Phase 2 adds the scan-table rendering, D-B3-2 marker mechanics, and re-scan triggering when the statement or the Confirmed set changes pre-clearing.

---

## T-10 · Solution surface review
Band 1 · serves: Solution · class: (canvas-internal) · target: canvas.md — Forms · Core Functions · Third-Party Connections · Localization sections (#1) · ★ —

### 1. Purpose & BABOK grounding
Completes the solution surface — Forms, Core Functions, Third-Party Connections, Localization — to the silence-fails discipline: each section filled or explicitly `N/A — <reason>`, every function naming the objective or vision element it serves, every connection carrying its role with direction stated where a source states one. Solution is the aspect that clears on canvas evidence alone (orchestrator D-O1, §2.1): this run is its whole first-pass surface — and the output is double ground: the connection rows seed the brief-§4 / CC-IN-01 chain, and the completed function and connection lines are the entity-implication surface T-11's domain model (B4) opens with (AT-RQ-4 — the batch's forward hand-off — D-B3-5, locked). The decision this technique lets the BA make: the surface is enumerated, each element knows why it exists, and what is deliberately unsettled is visibly open rather than silently absent. BABOK: 10.41 Scope Modelling · 10.20 Functional Decomposition · 10.24 Interface Analysis · 7.3 Validate Requirements (the linkage) · 4.3 Confirm Elicitation Results. House: plan §3 (Solution ← Vision); AT-SO-1/-2/-3 wording; orchestrator D-O1; elicitation §3.3 (the depth table that classifies direction settlement as scoping ground — D-B3-4's basis).

### 2. Catalogue metadata (BA-planning loop — orchestrator §6)
| Field | Value |
|---|---|
| Serves | Solution |
| Evidence triggers | AT-SO-1 — a section among Forms · Core Functions · Third-Party Connections · Localization empty or `open` with no `N/A — <reason>` ruling. AT-SO-2 — a core function names no objective or vision element (`→ O-n` / section-name link absent). AT-SO-3 — a connection row lacks its role, or its direction is neither stated nor explicitly open (D-B3-4, locked). |
| Skip-if | AT-SO-1…-3 read met in the current evidence table (a presale surface carried complete, links and rows at grade). Enrichment beyond the surface — capability inventories past the ≤ 10 line, market-by-market localization plans — only on BA ask (§6.1). |
| Depth | Elicits at surface grade: delivery forms; ≤ 10 capability lines (verb + object), each linked; connection rows at system · role · direction(-or-open) grade; localization as languages · currencies · regions. Must NOT solicit settlement of what the seed leaves open — source of truth, providers, sync semantics, failure ownership: their answers land in brief §4 / slicing rationale, and elicitation §3.3's own table classifies them as legal *scoping* questions — Tier-1 ground, above this run's canvas-grade destinations (principle 2; the BA may of course settle one deliberately) · model entities or relations (T-11, B4) · map journeys (T-13) · slice features or decompose epics (Band-2 ground, T-17/T-18) · specify UI or UX (T-14 governance / `/plan`). |
| Preconditions | **—** — canvas §§6–9 with the fresh Objectives and vision sections ground every contracted cell; `context.md` and `constraints.md` are read for the linkage sweep's plausibility and their absence costs no cell. |

### 3. Output contract (Q2+ — pre-pinned; orchestrator §6.3)
| Field | Value |
|---|---|
| Expected output | Canvas Forms · Core Functions · Third-Party Connections · Localization each filled or `N/A — <reason>`; every function linked `→ O-n` or to a vision element by section name (D-B3-3, locked); every connection row carrying role + direction stated-or-explicitly-open (D-B3-4, locked); function and connection lines in entity-ground form (EG-1, D-B3-5 — locked) |
| Artifact class | Context — the canvas's own class (#1, canvas-internal: no new artifact) |
| Destination file | `canvas.md` — §6 Forms · §7 Core Functions · §8 Third-Party Connections · §9 Localization (repo root; in the CC-H-01 estate from arming) |

### 4. Procedure
1. **BA act:** open Solution (T1 — Vision cleared or waived); compose the plan (P-O2); invoke T-10 (P-O3). D-O1 stands: Solution clears on canvas evidence alone — this run is the aspect's whole first-pass surface.
2. **Framework act:** pre-draft completion of §§6–9 from the framing-grade lines, presale material, kickoff notes, `context.md` (landscape ⇄ connection coherence) and `constraints.md` (the Forms line is drafted constraint-aware — a delivery form fighting a Confirmed row is a scan-grade defect caught at drafting), plus routed arrivals; cite-or-mark (principle 3); new function lines in capability form — verb + object, objects glossary-canonical (EG-1's ground).
3. **Framework act:** linkage sweep (AT-SO-2) — per function, `→ O-n` (session-fresh: T-08's lines) or a vision element by section name (D-B3-3, locked), proposed with basis where unstated. A function serving nothing is surfaced as such: the BA cuts it, or the missing objective is a proposed addition to §12 by batch (arrival is never gated; an addition to cleared Value ground routes, it does not reopen).
4. **Framework act:** connection pass (AT-SO-3) — per row: role always; direction stated where a source states it, otherwise explicitly `open — <what is unresolved>` (D-B3-4, locked). The open slot is deliberately preserved — it is the ready-made hole the Tier-1 kit converts to a must-ask (elicitation §8.1 Q4 is the worked case); the framework never solicits its settlement (depth rule above).
5. **BA act:** rule — sections, links, each `N/A — <reason>`, each open-direction row (silence fails; an explicit open is evidence, silence is not). Boundary routing + asymmetry: a connection implying an unrecorded today-system → `context.md` landscape proposal (T-05's ground); a binding discovery → `constraints.md` (T-06); a contradiction with cleared ground — an objective, the vision statement, a constraint, the cast — is a reopen signal (§3.5 step 4 → P-O6; by this run every upstream aspect is cleared, so the asymmetry's correction branch has gone quiet).
6. **Framework act:** write §§6–9 under the contract; refresh the Solution evidence table (§7.4) — AT-SO-1/-2/-3; confirmation is proposed when the table reads met. With Solution cleared, Requirements — the last gate — opens, and the completed §§7–8 stand as the entity-implication surface T-11 opens with (EG-1, D-B3-5 — locked).

### 5. Output template & micro-example
```markdown
Entity-ground clause (D-B3-5 — read by T-11, B4; plan §3 B3→B4 rationale):
EG-1 — The completed Core Function lines (verb + object, objects
       glossary-canonical) and Third-Party rows (system · role ·
       direction-or-open) are the entity-implication surface AT-RQ-4's
       "entities the core functions imply" resolves against; T-11 opens
       its sweep on exactly these lines — function objects as entity
       candidates, connection systems as boundary references.

| # | Section | Content — aspect grade |
|---|---|---|
| 6 | Forms | delivery formats — constraint-aware, or `N/A — <reason>` |
| 7 | Core Functions | ≤ 10 capability lines (verb + object), each `→ O-n` or `→ <vision section>` (D-B3-3) |
| 8 | Third-Party Connections | system — role; direction stated or `open — <what is unresolved>` (D-B3-4); or `N/A — <reason>` |
| 9 | Localization | languages · currencies · regions — each stated or `N/A — <reason>` |
```
Micro-example — Solution first pass (pre-RO-1):

| # | Section | Content |
|---|---|---|
| 6 | Forms | Responsive web application — phone-first for Clients (they book after work from phones); nothing installed at clinics (constraints §1) `[kickoff notes]` |
| 7 | Core Functions | Browse a Specialist's published Availability → O-2 · Book an available Slot → O-2 · Cancel own Appointment → Product.That · Notify the Specialist of bookings and cancellations → O-2 `[kickoff notes]` · Specialists publish their Availability → O-2 |
| 8 | Third-Party Connections | Specialists' external calendars — the Specialist's schedule lives there (context.md); booked Appointments must be visible in it; direction — open: source of truth for Availability unresolved `[context.md · kickoff notes]` |
| 9 | Localization | One interface language at launch — the network's home language; regions: the network's clinics, one country; currencies: `N/A — no payment surface in MVP scope` `[kickoff notes · canvas: Objectives O-1]` |

Continuity, four threads: **(i)** the publish line's `→ O-2` is exactly what survives RO-1 — "the function's objective link is unchanged; only its actor list grew" (orchestrator §12.3) — and the Cancel link exercises the D-B3-3 section-name notation. **(ii)** the open direction slot is kit Q4's ready-made hole and assumption A2's checking ground (elicitation §8.1); Solution clears over it (D-B3-4) — a direction-demanding reading would make Q4 illegal under Guard 1. **(iii)** Notify enters here from kickoff notes and is the line the E-03 roadmap description and brief §2 later carry (elicitation §8, §8.2). **(iv)** the function objects — Availability, Slot, Appointment — are the entity candidates T-11 opens on (EG-1).

### 6. Gate hooks
| Hook | IDs |
|---|---|
| Threshold hooks | AT-SO-1 · AT-SO-2 · AT-SO-3 |
| Health hooks | CC-H-01 — via `canvas.md` (no new file) |
| Consumer hooks | CC-IN-01 — chain consumer, never a direct read: the connection rows seed the Tier-1 pre-draft's brief §4 (elicitation §3.2 inputs), and CC-IN-01 compares brief §4 against the spec's Integration table (contract §5 C9; elicitation §4 map) — the canvas row is the chain's origin |

Cross-layer consumers, on the record: **AT-RQ-4 (T-11, B4)** opens on the EG-1 surface — the batch's forward hand-off (D-B3-5, locked) · Tier-1 kits read the surface (canvas is a §3.2 input; the open direction slot becomes the must-ask — elicitation §8.1 Q4) · decomposition (T-17, B6) consumes the discovery estate with this surface as its capability ground (plan §3 B6 rationale) · AT-SO-2's linkage is "the seed of the value chain briefs will later cite" (orchestrator §3.3).

### 7. Reference-design mining notes
No reference design — built to this sheet alone (elicitation §7.1 pattern).

### 8. Phase-2 build-brief hook
Invocation: `/ba-run T-10`; contract check at P-O3. Inputs loaded: `canvas.md` (§§6–9 + fresh Objectives and vision sections) · `.specify/memory/context.md` · `.specify/memory/constraints.md` · `glossary.md`. Interaction: pre-draft → linkage sweep → connection pass (stated-or-open) → destination-tagged questions → BA rulings incl. N/A and open-direction rulings → write + routed batches. Outputs written: `canvas.md` §§6–9, plus context/constraints batches where the passes find them. §§2–5 above fix the method; Phase 2 adds the linkage-sweep rendering, the EG-1 hand-off note toward T-11, and open-slot surfacing toward Tier-1 kit generation.

---

## Review record (v0.1 → v0.2)

Five decisions ruled by the BA Lead, 26 July 2026 — **all recommendations accepted as stated.** The locked text stands inline at each sheet; the table below stands as the rulings' record.

| # | Decision | Ruling (adopted as recommended) | Basis |
|---|---|---|---|
| **D-B3-1** | Line-ID continuation — D-B1-1 assigns `P-n`/`O-n` "at framing"; who IDs lines born later (this batch's additions, routed canvas edits)? | Post-framing writes extend each sequence monotonically — next unused `n`; a retired line's ID is never reused (the removal is recorded in the run log or batch; the number dies with the line). An interpretive extension of D-B1-1, which pins the scheme and the no-reuse rule; this pins the assigner for later lines. Mirror candidate for catalogue-b1 (T-01 / D-B1-1 note) at its next bump. | D-B1-1 (catalogue-b1); AT-VA-2, CC-OV-02 (pointability) |
| **D-B3-2** | The named-open-conflict instrument for AT-VI-3 — the criterion allows "resolved **or named as an open conflict** before clearing", but no spine document pins the naming's form | The D-B1-2 marker form at aspect grade: `[CONFLICT: vision claims <X> · constraints.md §<n> "<row>" binds <Y>]` on the affected statement line. Resolution paths: statement revised (normal) · constraint challenged → reopen signal on Context — cleared ground, so §3.5 step 4 → P-O6 applies and the Frame-time "no RO" clause does not — marker standing until RO resolution · both held → the marker **is** the naming; clearing over it is legal by the criterion's own text, the conflict visible on the canvas and in the clearing's evidence line. | AT-VI-3 wording; D-B1-2 (catalogue-b1); orchestrator §5.1, §9 |
| **D-B3-3** | Vision-element linkage notation for AT-SO-2 — the criterion allows a function to name "the objective **or vision element** it serves", but D-B1-1 numbered only Problems and Objectives | Link by section name — `→ Product.That` · `→ Our Solution` — no line-IDs added. The vision slots are single statements, so a section name is unambiguous; the notation extends `→ O-n` without touching D-B1-1's two-section scope ("nothing downstream cites [other sections] by line" — a section name is not a line cite). | AT-SO-2 wording; D-B1-1 (catalogue-b1) |
| **D-B3-4** | AT-SO-3's first-pass grade — "carries direction + role" — when direction is not yet stakeholder-settled at Band 1 | Every row carries its role; direction is stated where a source states it, otherwise explicitly `open — <what is unresolved>` — and an explicitly-open direction **satisfies first-pass grade**. Basis, fourfold: thresholds are minimum first-pass, not completeness (Q1b; orchestrator §3.2 rule 1); silence-fails demands the visible slot, not its settlement (rule 3); the direction question's answer is a scoping decision — elicitation §3.3's table classifies the E-03 source-of-truth question as legal **Tier-1** ground, so Band-1 generation may not solicit it (principle 2 at canvas grade); and the canonical timeline requires it — E-03's Solution cleared by 07-10 with kit Q4 (must-ask: "integration direction") still open at 07-14; a direction-demanding reading would make Q4 illegal under Guard 1. The open slot is the ready-made must-ask the kit converts. | Q1b; orchestrator §3.2/§3.3; elicitation §3.3, §8.1; Guard 1 |
| **D-B3-5** | The forward hand-off's form — how does T-10 pin the entity ground T-11 (B4) opens with? | One clause, stated in T-10 §5: **EG-1** — completed Core Function lines (verb + object, objects glossary-canonical) + Third-Party rows (system · role · direction-or-open) are the entity-implication surface AT-RQ-4 resolves against; T-11 opens its sweep on exactly these lines — function objects as entity candidates, connection systems as boundary references. Deliberately lighter than B2's TC-1…TC-3: the section shapes are already T-01-pinned; only the consumption relation needs pinning (D-B2-2's pin-at-the-producer precedent, at one-clause weight). | AT-RQ-4; plan §3 B3→B4 rationale; D-B2-2 (catalogue-b2) |

**Conflict scan — against the five spine documents (standard v0.3 · contract v0.2 · elicitation v0.3 · gate v0.2 · orchestrator v0.2) and prior batches (catalogue-b1 v0.2 · catalogue-b2 v0.2).** Checked: AT-VA-1/-2, AT-VI-1/-2/-3, AT-SO-1/-2/-3, AT-RQ-4 wording (orchestrator §3.3) · CC-OV-02, CC-IN-01, CC-H-01 wording and read-sets (contract §5–§6; CC-IN-01 reads spec+brief — the sheets claim chain consumption only, never a direct read; the category cite corrected to C9 at review) · destination sections against T-01's 13-section table (§§2–9, 11–12 all resolve; `canvas.md` at repo root inside CC-H-01's glob; no new file claimed anywhere) · aspect ⇄ section anchoring against plan §3 · the B2 seed contracts (D-B2-3 path `.specify/memory/competitive-analysis.md`; D-B2-4's Confirmed-only mechanical read) and B2's boundary lines honored in both directions (T-07 supplies targets, never the statement — T-09 authors it; T-06 §6 names T-09 as AT-VI-3's reader — this batch is that reader) · micro-example continuity: pre-RO-1 state held — the publish line's actor list and its surviving `→ O-2` link match orchestrator §12.3's re-clear exactly ("only its actor list grew; the function's objective link is unchanged"); P-1/O-1/O-2 text carried from catalogue-b1's framing excerpt; the differentiation names T-07's entries and leans on its keyed deltas; the scan runs against T-06's three Confirmed rows; the open direction slot reconciles with kit Q4/A2 (elicitation §8.1) and the Notify line with the roadmap description and brief §2 (elicitation §8, §8.2); O-2 is the Value Anchor's citation target (§8.2 §1). Three reconciliations on record, none a contradiction: **(a)** D-B3-4's stated-or-explicitly-open reading of AT-SO-3 is forced by the canonical timeline — Solution cleared by 07-10 with kit Q4 still open on 07-14; a direction-demanding reading would make Q4 illegal under Guard 1, and elicitation §3.3's table classifies that question as Tier-1 ground. **(b)** D-B3-1 reads D-B1-1's "assigned at framing" as pinning the scheme and the no-reuse rule, with continuation as the assigner for later lines — an interpretive extension, not a conflict. **(c)** forward references to T-11/T-13/T-14/T-17/T-18 stand at inventory identity only (the B2 scan's precedent (b)); the sequencing invariant — no future sheet *content* cited — is intact; EG-1 pins what T-11 will consume, the D-B2-2 producer-pin pattern. Two additive extensions, flagged for the record as **mirror candidates at catalogue-b1's next version bump: D-B3-1** (a continuation clause beside D-B1-1 / T-01 step 3) **and D-B3-3** (T-01 §5's Core Functions cell gains the `→ <vision section>` notation beside `→ O-n`). **No spine contradiction found; no erratum issued.**

---

*v0.2 · review incorporated 26 July 2026 · sheets T-08–T-10 of 18 · authored to sequencing-plan §2 template (D-W9 order: contract before procedure) · no ★ in batch — all three to the elicitation-§7.1 no-reference pattern · canvas-internal: no new artifact; contracts land in `canvas.md` sections (#1, Context) · B2 seeds consumed: Confirmed rows → AT-VI-3 · Unlike entries + keyed deltas → AT-VI-2 · forward hand-off pinned: EG-1 (D-B3-5) for T-11 (B4) · decisions D-B3-1…D-B3-5 locked (review record above) · pending mirrors at catalogue-b1's next bump: D-B3-1 · D-B3-3 · consumed by: batches B4–B6 (cite, never restate) · next: B4 — Requirements core chain (T-11…T-13), standing batch prompt §4, new conversation*
